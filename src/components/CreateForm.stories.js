import React from 'react'
import CreateForm from './CreateForm'

export default {
  title: 'CreateForm',
  component: CreateForm,
}

const Template = () => <CreateForm />

export const Form = Template.bind({})
